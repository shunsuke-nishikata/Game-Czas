<% if event.requests_by?(current_user) %>
  <%= link_to "リクエスト済み", event_request_path(event.id, event.requests),method: :delete, remote: true, class: "btn btn-primary request-button" %></br>
<% else %>
  <%= link_to "リクエスト!!", event_requests_path(event.id),method: :post,remote: true, class: "btn btn-primary request-button" %></br>
<% end %>